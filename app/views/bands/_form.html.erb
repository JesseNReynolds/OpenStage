<%= form_with(model: band, local: true) do |form| %>
  <% if band.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(band.errors.count, "error") %> prohibited this band from being saved:</h2>

      <ul>
        <% band.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label "Name" %>
  <%= form.text_field :name %>
  <br>
  <%= form.label "Description" %>
  <%= form.text_area :bio, placeholder: "We play bluegrass and post-psychadelic-funk-metal." %>
  <br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
